import React from 'react';

//Make tweet component

const MeowBig = (event) => {
  // const [bigTweet, setBigTweet] = useState();

  return (
    // const bigtweetResponse = await fetch(`/api/tweet/${tweetId}`, {
    //   ((res) => res.json())
    //   ((data) => {
    //     setBigtweet(data.tweet);
    //     setTweetAuthor(data.tweet.author);
    //     setMedia(data.tweet.media[0]);
    //   })
    // })
    console.log('hi')
  )
};

// useEffect(() => {
//   fetch(/api/tweet/${tweetId})
//         .then((res) => res.json())
//         .then((data) => {
//           setBigtweet(data.tweet);
//           setTweetAuthor(data.tweet.author);
//           setMedia(data.tweet.media[0]);
//         });
// }, []);

export default MeowBig;
